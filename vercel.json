{
  "outputDirectory": "dist",
  "builds": [{ "src": "/dist/*.js", "use": "@vercel/node" }],
  "rewrites": [{ "source": "/(.*)", "destination": "/dist" }],
  "buildCommand": "npm run build",
  "devCommand": "npm run dev"
}
